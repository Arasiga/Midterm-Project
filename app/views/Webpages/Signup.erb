
<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>Sign up page</title>
</head>
<body>

<div class="vid-container">

  <video id="Video1" class="bgvid back" autoplay="false" muted="muted" preload="auto" loop>
      <source src="https://s3-us-west-2.amazonaws.com/coverr/mp4/Aloha-Mundo.mp4" type="video/mp4">
  </video>
  
  <h1><font color="white"> Sign up for CodePad </font></h1>

  <div class="container_signup">
  <div class="box_signup">
    <h3> Please provide the following to create your account </h3>
    <% if @user && @user.errors.any? %>
    <div>
      <strong> Some issues while creating account, please fix the following:
      </strong>
      <ul>
        <% @user.errors.full_messages.each do |x| %>
        <li><%= x %></li>
        <% end %>
      </ul>
    </div>
    <% end %>
    <form action="/Webpages/Signup" method="post" id="signup_form">
      <ul style="list-style-type: none">
        <li>
          <label>First Name:</label><input id="first_name" type="text" name="first_name" placeholder="First name" value="<%= @user && @user.first_name%>" required/>
        </li>
        <li>
          <label>Last Name:</label><input id="last_name" type="text" name="last_name" placeholder="Last name" value="<%= @user && @user.last_name%>" required/>
        </li>
        <li>
          <label> Country:</label><input id="country" type="text" name="country" placeholder="Country" value="<%= @user && @user.country%>" required/>
        </li>
        <li>
          <label>Email: </label><input id="email" type="email" name="email" placeholder="Email" value="<%= @user && @user.email%>" required/>
        </li>
        <li>
          <label>Username: </label><input id="username" type="text" name="username" placeholder="Enter a username that others can identify you with" value="<%= @user && @user.username%>" required/>
        </li>
        <li>
          <label>Password:</label><input id="password" type="password" name="password" placeholder="Password" required/>
        </li>
        <li>
          <label>Repeat password:</label><input id="repeat_password" type="password" name="repeat_password" placeholder="Please confirm password" equalTo: "password" required/>
        </li>
        <button> Create Account! </button>
        <p><a href="/Webpages/Signin"><span> About us</span></a></p>
      </div>
    </ul>
  </form>
</div>
</div>
</div>

</body>
</html>














